import { TextButtonAnimationDirective } from './text-button-animation.directive';

describe('TextButtonAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new TextButtonAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
